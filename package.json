{
  "name": "serene",
  "version": "1.0.0",
  "scripts": {
    "prod": "npx mix --production",
    "pack": "npx mix --production && mkdir -p pack/src/util && bash -c 'cp -v composer.json style.css *.php -r {assets,build,views,vendor} -t pack && cp -v src/util/*.php -t pack/src/util && cp -v src/*.php -t pack/src'"
  },
  "author": "em",
  "license": "MIT",
  "dependencies": {
    "alpinejs": "^3.14.1",
    "autoprefixer": "^10.4.20",
    "laravel-mix": "^6.0.49",
    "postcss": "^8.4.47",
    "sass": "^1.79.2",
    "tailwindcss": "^3.4.12"
  },
  "devDependencies": {
    "resolve-url-loader": "^5.0.0",
    "sass-loader": "^12.6.0"
  }
}
